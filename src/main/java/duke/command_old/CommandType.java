package duke.command_old;

/**
 * Types of commands currently in use.
 */
public enum CommandType {
    LIST_TASKS, MARK_DONE, ADD_TODO, ADD_DEADLINE, ADD_EVENT, DELETE_TASK, HELP, GREETING, EXIT, DEFAULT
}
