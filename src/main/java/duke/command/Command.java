package duke.command;

//lists all the types of commands currently in use
public enum Command {
    LIST_TASKS, MARK_DONE, ADD_TODO, ADD_DEADLINE, ADD_EVENT, REMOVE_TASK, HELP, GREETING, EXIT, DEFAULT
}
