package duke.command;

/**
 * Contains all the possible commands the user can give stored as constants.
 */
public enum Command {
    EXIT,
    LIST,
    ADD_TO_DO,
    ADD_DEADLINE,
    ADD_EVENT,
    DONE,
    INVALID,
    DELETE,
    FIND
}
