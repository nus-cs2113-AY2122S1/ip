package duke;

/**
 * An enum class Command that contains the various commands that Duke recognises.
 */
public enum Command {
    EXIT,
    LIST,
    FIND,
    CLEAR,
    ADD_TODO,
    ADD_DEADLINE,
    ADD_EVENT,
    DO_TASK,
    DELETE,
    INVALID
}