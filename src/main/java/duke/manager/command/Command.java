package duke.manager.command;

/**
 * <h1>Command</h1>
 * <code>Command</code> is an enum that represent various commands in a more intuitive manner.
 */
public enum Command {
    ADD_EVENT, ADD_DEADLINE, ADD_TODO, DELETE_TASK,
    SHOW_LIST, DONE_TASK, EXIT, INVALID
}
